package vji.mvc;
import vji.chess.mvc.ChessControllerImpl;
import vji.mvc.*;

/**
 *
 * @author vji
 */
public class AppEndCommandImpl implements AppEndCommand {

    @Override
    public ModelAndView process(Controller controller) {
        return ((Controller) controller).processAppEndCommand(this);
    }
}
